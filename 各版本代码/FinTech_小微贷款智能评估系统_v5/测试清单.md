# 测试清单 — FinTech 小微贷款智能评估系统 v5

## 后端功能测试

### 基础 API
- [ ] `GET /api/health` 返回 `{"status":"healthy","ai_available":true/false}`
- [ ] `GET /api/banks` 返回 30 家银行产品列表

### 贷款评估
- [ ] `POST /api/evaluate` 传入完整 LoanInput → 返回 score + 5维分解 + 30家银行匹配
- [ ] `POST /api/evaluate` 缺少必填字段 → 返回 422 Validation Error
- [ ] `POST /api/evaluate/quick` 返回精简版（TOP5银行）

### AI 对话 (需 DEEPSEEK_API_KEY)
- [ ] `POST /api/chat` 问"Aave风险怎么样" → Agent 自动调工具获取数据后回复
- [ ] `POST /api/chat` 问"制造业50万贷款有什么政策支持" → 返回政策列表 + 银行推荐
- [ ] `POST /api/chat/reset` 重置会话后 → 历史清空
- [ ] 无 API Key 时 `/api/chat` → 降级模式提示

### 企业搜索
- [ ] `POST /api/enterprise/search` 搜"餐饮" → 返回匹配企业列表

### 知识库工具
- [ ] `GET /api/health` 确认 kb_bridge 成功加载
- [ ] `search_policies("制造业")` → 返回 ≥1 条政策
- [ ] `search_cases(industry="餐饮")` → 返回 ≥1 条案例

### ML 模型
- [ ] `POST /api/evaluate` 返回 `ml_enhanced: true` 且 `ml_default_prob` 不为 null
- [ ] ML 模型缺失时 → `ml_enhanced: false`，不崩溃

---

## 前端功能测试

### 仪表盘
- [ ] 页面加载 → 显示 8 个 Demo Case 按钮
- [ ] 点击 Case → 评估结果实时更新（本地计算）
- [ ] 评分仪表盘渲染正常（0-100 环形图）
- [ ] 五维雷达图对比 TOP5 银行
- [ ] 材料清单显示完整（基础/财务/增强分类）

### AI 对话
- [ ] 输入问题 → 收到回复（有 Key 时 LLM 回复，无 Key 时降级提示）
- [ ] 快捷问题按钮 → 自动发送
- [ ] 多轮对话 → 上下文保持一致
- [ ] 后端未启动时 → 显示连接失败提示

### 供应链风险
- [ ] 供应链图渲染正常（热力图颜色区分）

---

## 部署测试

### 后端启动
- [ ] `pip install -r requirements.txt` 无报错
- [ ] `python train_ml_enhanced.py` 模型训练成功
- [ ] `python main.py` 启动成功，显示 "AI Mode"
- [ ] API 文档 http://localhost:8000/docs 可访问

### 前端启动
- [ ] `npm install` 无报错
- [ ] `npm run dev` 启动成功
- [ ] http://localhost:3000 可访问

### 环境变量
- [ ] .env 不存在时 → Chat 降级模式
- [ ] .env 含正确 Key → AI 模式启用
- [ ] .env.example 内容与代码一致

---

## 数据质量测试

- [ ] kb/data/ 16个文件全部可读
- [ ] bank_products.json → banks 数组长度 = metadata.total_banks
- [ ] national_policies.csv ≥ 18 条记录
- [ ] teaching_cases_enhanced.csv ≥ 20 条记录
- [ ] 所有 CSV 为 UTF-8 BOM 编码
- [ ] kb/VERSION 数据与实际情况一致

---

## Git 规范测试

- [ ] .gitignore 已排除 .env, *.pkl, node_modules/
- [ ] 无 .env 文件被追踪
- [ ] 无 .pkl 文件被追踪
- [ ] commit 消息符合前缀规范
